# NPN_HACKATHON — API RUNTIME dependencies (minimal).
#
# This is everything the FastAPI service needs to serve every endpoint except
# live inference. Verified against the actual imports in app/ — the API core
# deliberately contains no ML libraries, which keeps the container small and
# startup under a second.
#
# Live inference additionally needs requirements-inference.txt.
# Development/testing additionally needs requirements-dev.txt.
#
# Python 3.11+ (developed and verified on 3.13.14).

fastapi==0.135.1
uvicorn[standard]==0.42.0
pydantic==2.12.5
pydantic-settings==2.7.0
duckdb==1.5.5

# --- AI assistant (optional at runtime) ---
# The service degrades gracefully when no provider key is set. Groq has no
# first-party SDK dependency here — it's a plain OpenAI-compatible REST call,
# made directly with httpx.
httpx==0.28.1
